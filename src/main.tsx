import React from 'react'
import ReactDOM from 'react-dom/client'
import CRMVentapel from './CRMVentapel'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
