node_modules
dist
# Build de medição do QA (scripts/medir-arranque.mjs). Nunca é o que se publica.
dist-qa
dist-ssr
.vite
*.local

# Segredos
.env
.env.*
!.env.example

# Android / TWA — o projeto Bubblewrap se gera; o keystore NUNCA entra no repo.
# `android/*` ignora tudo e as exceções abaixo devolvem só o que é código-fonte.
android/*
!android/.gitkeep
!android/twa-manifest.json
!android/README.md
*.keystore
*.jks
*.apk
*.aab

# Sistema / editor
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json

# Testes e cobertura
coverage
.vitest
*.log
npm-debug.log*

# Rascunhos de depuração do Playwright
e2e/_tmp/
